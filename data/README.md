Data directory

- documents/: raw documents (optional, ignored in VCS)
- processed/: intermediate artifacts (optional, ignored in VCS)
- vectorstore/: embeddings index (optional, ignored in VCS)

These folders are optional at runtime; kept for clarity and as mount points for Docker/Compose.
